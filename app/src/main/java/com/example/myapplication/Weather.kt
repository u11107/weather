package com.example.myapplication

data class Weather(val day:String,
                   val cloudiness: Boolean,
                   val rain: Boolean,
                   val windy: Boolean,
                   val temp:Short)
